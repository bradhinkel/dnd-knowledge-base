# Phase 1 variant A: metadata filtering OFF (matches baseline)
name: phase1_filter_off
description: "Phase 1 — no metadata filtering; chunk 1000/200, ada-002, vector, top-5"

chunking:
  size: 1000
  overlap: 200

embedding:
  model: text-embedding-ada-002

retrieval:
  method: vector
  top_k: 5
  hybrid_alpha: null

metadata_filter: false
reranking: false
