# Phase 3 variant C: embedding model BGE (local, HuggingFace)
name: phase3_bge
description: "Phase 3 — embedding BAAI/bge-small-en-v1.5 (local); best chunk from Phase 2"

chunking:
  size: 512    # placeholder — update with best chunk size from Phase 2
  overlap: 100

embedding:
  model: BAAI/bge-small-en-v1.5
  # provider: huggingface (local, no API cost)

retrieval:
  method: vector
  top_k: 5
  hybrid_alpha: null

metadata_filter: true
reranking: false
