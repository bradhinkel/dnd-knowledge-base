# Enforce LF for files that are executed/parsed on the Linux droplet.
# Prevents Windows CRLF from breaking bash (`set -o pipefail`) and systemd units.
*.sh            text eol=lf
*.service       text eol=lf
deploy/nginx/*  text eol=lf
