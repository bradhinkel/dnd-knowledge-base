# Phase 4 variant B: search method = BM25 keyword only
name: phase4_bm25
description: "Phase 4 — BM25 keyword search only; best embedding from Phase 3"

chunking:
  size: 512    # placeholder — update with best chunk size from Phase 2
  overlap: 100

embedding:
  model: text-embedding-ada-002  # placeholder — update with best embedding from Phase 3

retrieval:
  method: bm25
  top_k: 5
  hybrid_alpha: null

metadata_filter: true
reranking: false
